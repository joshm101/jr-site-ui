const styles = theme => ({
  listItem: {
    '&:hover': {
      cursor: 'pointer'
    }
  },
  noPostsNoticeLabel: {
    marginBottom: theme.spacing(10)
  },
  root: {
    width: '100%',
    height: '100%'
  }
})

export default styles
