const styles = theme => ({
  contentRoot: {
    minHeight: theme.spacing(40),
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },
  form: {
    width: '100%'
  }
})

export default styles
